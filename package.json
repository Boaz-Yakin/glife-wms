{
  "name": "wms-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "mobile-app",
    "web-app"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "lint": "turbo run lint"
  },
  "devDependencies": {
    "turbo": "^2.0.0"
  },
  "packageManager": "npm@10.8.2"
}
